# 功能模块名称

> 说明这个模块的作用以及模块的目录。

## Context

此部分描述这个模块的功能。

## Model

这一部分描述这个模块的新增或者是用到的数据模型/实体类。

## Backend API

描述后端新增了哪些 API，按照以下格式编写。

| Method | Path      | 说明          |
|--------|-----------|-------------|
| `POST` | `/agents` | 创建 Agent 配置 |

## Runtime Flow

这里描述每个功能的运行流程。

## Validation

描述如何验证这个功能是正常的。

## Known Limits

描述这个功能模块有哪些已知限制与工程实现上的妥协，包括但不限于：

- 与用户沟通后用户确认的可以暂不实现的功能 / 模块
- 用户已知晓的`受限于当前代码结构`或是`修改工作量过大`而产生的妥协设计
- 受限于技术方案暂时无法实现的功能
